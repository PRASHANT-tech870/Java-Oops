package access;

public class Main {
    public static void main(String[] args) {
        /*

                public -> class/any class of same package/subclass of same package/ subclass of different package/ any class of different package
                protected -> class/any class of same package/ subclass of same package/ subclass of different package
                no modifier/ default -> class/any class of same package/ subclass of same package
                private -> class

                hashcode method is not address it is some random integer from a algorithm
                equals method checks the value of the object
                instanceof checks if an object is instanceof a class
                getClass() gives info about the class of an object
         */
    }
}
